# name: build-docker
# run-name: Build and deploy docker images to hub

# on: 
#   push:
#     paths:
#       - apps
#       - packages
#     branches:
#       - main

# concurrency:
#   group: ${{ github.workflow }}-${{ github.ref }}
#   cancel-in-progress: true

# jobs:
#   apps_images:
#     name: build and publsh apps images
#     runs-on: ubuntu-latest
#     steps:
#       - name: Login to Docker Hub
#         uses: docker/login-action@v4
#         with:
#           username: ${{ secrets.DOCKERHUB_USERNAME }}
#           password: ${{ secrets.DOCKERHUB_TOKEN }}

#       - name: Set up QEMU
#         uses: docker/setup-qemu-action@v4
      
#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v4
      
#       - name: Build and push backend
#         uses: docker/build-push-action@v7
#         with:
#          file	String	Path to the Dockerfile. (default {context}/Dockerfile)
#           context: .
#           file: ./apps/backend/Dockerfile
#           push: true
#           tags: |
#             tarunsingh28/sky-backend:latest
#             tarunsingh28/backend:${{ github.sha }}

#       - name: Build and push frontend
#         uses: docker/build-push-action@v7
#         with:
#           context: ./apps/frontend
#           push: true
#           tags: |
#             tarunsingh28/sky-frontend:latest
#             tarunsingh28/frontend:${{ github.sha }}


    
  
